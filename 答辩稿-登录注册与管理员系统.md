# 校园置换系统 —— 登录注册 & 管理员系统答辩稿

> 技术栈：Spring Boot 3.x · MyBatis-Plus · MySQL · JWT · BCrypt · Hutool

---

## 一、登录注册系统

### 1.1 整体流程

#### 注册流程

```
用户输入邮箱
    ↓
POST /api/user/send-code  →  生成6位验证码  →  QQ邮箱SMTP发送
    ↓
用户填写 邮箱 + 验证码 + 昵称 + 密码
    ↓
POST /api/user/register
    ↓
校验验证码有效性  →  校验邮箱唯一性  →  BCrypt加密密码  →  写入数据库
```

#### 登录流程

```
用户输入邮箱 + 密码
    ↓
POST /api/user/login
    ↓
查询用户  →  BCrypt比对密码  →  检查账号状态（是否禁用）
    ↓
生成 JWT Token（有效期24小时）
    ↓
返回 { token, userInfo }
```

#### 忘记密码流程

```
POST /api/user/forgot-password
    ↓
校验邮箱验证码  →  BCrypt加密新密码  →  更新数据库
```

---

### 1.2 接口一览

| 方法 | 路径 | 说明 | 是否需要登录 |
|------|------|------|:-----------:|
| POST | `/api/user/send-code` | 发送邮箱验证码 | 否 |
| POST | `/api/user/register` | 用户注册 | 否 |
| POST | `/api/user/login` | 用户登录 | 否 |
| POST | `/api/user/forgot-password` | 忘记密码 | 否 |
| GET  | `/api/user/profile` | 获取个人信息 | 是 |
| PUT  | `/api/user/profile` | 更新个人信息 | 是 |
| PUT  | `/api/user/password` | 修改密码（旧密码验证） | 是 |
| PUT  | `/api/user/password/by-email` | 修改密码（邮箱验证） | 是 |
| POST | `/api/user/logout` | 退出登录 | 是 |

---

### 1.3 重难点一：验证码防滥用设计

**问题背景**：验证码发送接口若不加限制，恶意调用会导致邮件轰炸，消耗邮件服务资源。

**解决方案**：使用 Hutool `TimedCache` 实现**双缓存机制**。

```
codeKey  = "email:scene"      → 存储验证码，5分钟后自动失效
coolKey  = "cool:email:scene" → 存储冷却标记，60秒后自动失效
```

**核心逻辑**：

```
发送验证码请求进入
    ↓
检查 coolKey 是否存在？
    ├── 存在  → 拒绝，返回"请60秒后再试"
    └── 不存在 → 生成6位随机数字码
                 → 设置 codeKey（5分钟有效）
                 → 设置 coolKey（60秒有效）
                 → 发送邮件
```

**验证逻辑**：

```
校验验证码
    ↓
从 codeKey 取出存储值 → 与用户输入比对
    ├── 不匹配 → 返回"验证码错误"
    └── 匹配   → 立即删除 codeKey（防重放攻击）→ 返回验证通过
```

> **关键点**：验证即销毁，同一验证码只能使用一次；60秒冷却期防止频繁发送。

---

### 1.4 重难点二：JWT 无状态鉴权

**传统 Session 的局限**：服务端需维护 Session 状态，不利于水平扩展。

**JWT 方案原理**：

```
JWT = Header（算法）. Payload（载荷）. Signature（签名）

Payload 包含：
  - sub: userId（用户ID）
  - iat: 签发时间
  - exp: 过期时间（24小时后）

签名算法：HS256
密钥：32位以上固定密钥
```

**请求鉴权流程**（由 `JwtInterceptor` 实现）：

```
HTTP 请求进入
    ↓
读取请求头：Authorization: Bearer <token>
    ↓
是否为公开接口白名单？
    ├── 是 → 直接放行
    └── 否 → 校验签名 + 过期时间
              ├── 无效/过期 → 401 UNAUTHORIZED
              └── 有效 → 解析 userId，存入 Request 属性 → 放行
                         Controller 通过 request.getAttribute("userId") 获取当前用户
```

**公开接口白名单**：

- `POST /api/user/login`
- `POST /api/user/register`
- `POST /api/user/send-code`
- `POST /api/user/forgot-password`
- `GET /api/item/list`（商品列表，游客可浏览）
- `GET /api/item/{id}`（商品详情，游客可查看）

---

### 1.5 重难点三：密码安全存储

**为什么不用 MD5 / SHA？**

| 算法 | 问题 |
|------|------|
| MD5 | 有彩虹表，可逆推 |
| SHA-256 | 无盐值，相同密码哈希相同，易被撞库 |
| **BCrypt** | 内置随机盐值，同密码每次加密结果不同，无法逆推 |

**使用方式**：

```java
// 注册：加密存储
String encoded = BCrypt.hashpw(rawPassword, BCrypt.gensalt());

// 登录：比对（无需解密）
boolean match = BCrypt.checkpw(rawPassword, encodedPassword);
```

---

## 二、管理员系统

### 2.1 权限模型设计

#### 角色等级

| role 值 | 角色 | 描述 |
|:-------:|------|------|
| 0 | 普通用户 | 无任何管理权限 |
| 1 | 普通管理员 | 拥有超管授予的指定权限 |
| 2 | 超级管理员 | 拥有所有权限，无需额外授权 |

#### 权限码（存于 `t_admin_permission` 表）

| 权限码 | 功能 |
|--------|------|
| `USER_MANAGE` | 用户管理：查询、禁用/启用用户 |
| `ITEM_MANAGE` | 商品管理：下架、强制删除商品 |
| `ITEM_AUDIT` | 商品审核：通过或拒绝待审商品 |

#### 数据库表结构

```sql
-- 用户表（部分字段）
t_user
  id        BIGINT PRIMARY KEY
  email     VARCHAR(100) UNIQUE   -- 登录账号
  password  VARCHAR(255)          -- BCrypt密文
  role      TINYINT               -- 0/1/2
  status    TINYINT               -- 0-正常 / 1-禁用
  deleted   TINYINT               -- 逻辑删除

-- 管理员权限表
t_admin_permission
  id        BIGINT PRIMARY KEY
  admin_id  BIGINT    -- 关联 t_user.id（role=1的用户）
  perm_code VARCHAR   -- USER_MANAGE / ITEM_MANAGE / ITEM_AUDIT
  created_at DATETIME
```

---

### 2.2 管理员接口一览

#### 用户管理（需 USER_MANAGE 权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/users` | 分页查询用户（支持邮箱/昵称搜索） |
| PUT | `/api/admin/users/{id}/status` | 禁用/启用用户 |
| PUT | `/api/admin/users/{id}/role` | 修改用户角色（**仅超管**） |
| DELETE | `/api/admin/users/{id}` | 删除用户（**仅超管**，逻辑删除） |

#### 商品管理（需 ITEM_MANAGE 权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/items` | 分页查询所有商品 |
| PUT | `/api/admin/items/{id}/status` | 修改商品状态 |
| DELETE | `/api/admin/items/{id}` | 强制删除商品（逻辑删除） |

#### 商品审核（需 ITEM_AUDIT 权限）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/items/pending` | 获取待审核商品列表 |
| PUT | `/api/admin/items/{id}/audit` | 审核商品（通过/拒绝） |

#### 权限管理（**仅超管**）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/admins` | 查询管理员列表 |
| GET | `/api/admin/admins/{id}/permissions` | 获取某管理员的权限 |
| PUT | `/api/admin/admins/{id}/permissions` | 设置某管理员的权限 |
| GET | `/api/admin/me/permissions` | 查询自己的权限 |

---

### 2.3 重难点四：双层拦截器链

拦截器执行顺序：`JwtInterceptor` → `RoleInterceptor`

```
HTTP 请求  →  [JwtInterceptor]            →  [RoleInterceptor]
               验证 Token，解析 userId        验证 role >= 1
               存入 Request 属性              存入 userRole 到 Request 属性
                    ↓                              ↓
               身份不合法 → 401             非管理员 → 403
                    ↓                              ↓
                  放行                           放行（进入 Controller）
```

**Controller 内部细粒度权限检查**：

```
从 Request 获取 userRole
    ↓
role >= 2（超管）→ 直接放行
    ↓
role == 1（普通管理员）→ 查 t_admin_permission 表
    ├── 有对应权限码 → 执行业务
    └── 无对应权限码 → 403 FORBIDDEN
```

> **设计原则**：拦截器负责"是否是管理员"，Controller 负责"有没有这个功能权限"，职责分离，易于维护。

---

### 2.4 重难点五：管理操作的安全边界

管理员操作其他用户时，需防止"越权"和"自操作"问题。

#### 禁用/启用用户规则

```
操作者 role=1（普通管理员）
    → 只能操作 role=0 的普通用户
    → 不能操作其他管理员或超管
    → 不能操作自己

操作者 role=2（超级管理员）
    → 可操作任意用户
    → 不能操作自己
```

#### 角色修改规则

```
仅超管（role=2）可修改用户角色
    → 不允许将用户提升为超管（role=2）
      （超管账号只能在数据库直接维护，防止权限滥用）
```

#### 管理员权限设置规则

```
仅超管可设置普通管理员的权限
    → 采用全量覆盖策略：
      1. 删除该管理员所有旧权限记录
      2. 批量插入新权限记录
    → 传入空列表 = 清除所有权限（降为"无权限管理员"）
```

---

### 2.5 重难点六：商品审核状态机

商品发布后经历一个完整的审核生命周期：

```
用户发布商品
    ↓
auditStatus = 0（待审核）← 前台不展示，用户可见"审核中"
    ↓
管理员进入待审核列表
    ↓
    ├── action=1（通过）→ auditStatus=1，status=0（在售），前台展示
    └── action=2（拒绝）→ auditStatus=2，必须填写 remark（拒绝原因）
                          用户可见拒绝原因，修改后重新发布
```

**商品状态（status）独立控制**：

| status 值 | 含义 |
|:---------:|------|
| 0 | 在售（正常展示） |
| 1 | 已下架（管理员操作） |
| 2 | 已售出（卖家标记） |

> auditStatus 和 status 是两个独立维度：审核通过后商品才进入 status 流转；管理员可随时将已通过的商品下架。

---

## 三、统一响应与异常处理

### 3.1 统一响应格式

所有接口均返回统一结构：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### 3.2 状态码规范

| code | 含义 | 场景 |
|:----:|------|------|
| 200 | 成功 | 正常业务响应 |
| 400 | 请求参数错误 | 参数校验失败（@Valid） |
| 401 | 未认证 | Token 缺失/过期/无效 |
| 403 | 无权限 | 角色或权限码不满足 |
| 404 | 资源不存在 | 查询的对象不存在 |
| 500 | 服务器内部错误 | 未捕获异常 |

### 3.3 全局异常处理器

`GlobalExceptionHandler` 集中处理四类异常：

```
BusinessException     → 业务异常，返回自定义错误码和消息
@Valid 校验失败        → 400，返回具体字段错误信息
NoResourceFoundException → 404
Exception（兜底）      → 500，记录日志
```

---

## 四、可能被追问的问题

**Q：JWT Token 过期后如何处理？**

> 客户端收到 401 响应后，引导用户重新登录。如需"无感刷新"，可引入 Refresh Token 机制：短期 Access Token（30分钟）+ 长期 Refresh Token（7天），用户无感知地续期。这是本项目后续可优化的点。

**Q：JWT 如何实现强制退出（如用户被封禁）？**

> JWT 是无状态的，服务端默认无法主动使 Token 失效。解决方案是引入 Redis 黑名单：封禁用户时将其 Token 加入 Redis 黑名单，拦截器每次都检查黑名单。本项目为简化架构未引入 Redis，但这是生产级系统的标准做法。

**Q：验证码为什么不用 Redis 存储？**

> 本项目是单机部署，Hutool `TimedCache` 基于内存，足够轻量，无需额外中间件。若要改为分布式部署，只需将验证码存储层替换为 Redis，对外接口不变，改动最小化。

**Q：为什么用逻辑删除而不是物理删除？**

> 逻辑删除（`deleted=1`）保留了数据记录，便于审计追踪和误操作恢复。MyBatis-Plus 配置 `logic-delete-field=deleted` 后，会自动在所有查询中追加 `WHERE deleted=0`，对业务代码完全透明，无需每处手动过滤。

**Q：BCrypt 会不会影响登录性能？**

> BCrypt 的计算复杂度是设计上的权衡，默认 cost=10，单次验证约耗时 100ms 左右，对登录接口完全可接受。正因为它"慢"，才使暴力破解的成本极高。

---

## 五、核心文件索引

| 功能 | 文件路径 |
|------|---------|
| 用户实体 | `entity/User.java` |
| 用户业务逻辑 | `service/impl/UserServiceImpl.java` |
| 用户接口 | `controller/UserController.java` |
| 验证码服务 | `service/impl/VerifyCodeServiceImpl.java` |
| JWT 工具类 | `utils/JwtUtils.java` |
| JWT 拦截器 | `interceptor/JwtInterceptor.java` |
| 角色拦截器 | `interceptor/RoleInterceptor.java` |
| 拦截器注册 | `config/WebConfig.java` |
| 管理员业务逻辑 | `service/impl/AdminServiceImpl.java` |
| 管理员接口 | `controller/AdminController.java` |
| 管理员权限实体 | `entity/AdminPermission.java` |
| 全局异常处理 | `common/exception/GlobalExceptionHandler.java` |